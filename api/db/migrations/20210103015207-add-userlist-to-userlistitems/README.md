# Migration `20210103015207-add-userlist-to-userlistitems`

This migration has been generated by Greg Murray at 1/2/2021, 8:52:07 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "UserListItem" DROP CONSTRAINT "UserListItem_userListId_fkey"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210103015008-add-userlist-to-userlistitem..20210103015207-add-userlist-to-userlistitems
--- datamodel.dml
+++ datamodel.dml
@@ -1,26 +1,25 @@
 datasource DS {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
   binaryTargets = "native"
 }
 model List {
-  id              Int            @id @default(autoincrement())
-  isPublic        Boolean        @default(false)
+  id              Int        @id @default(autoincrement())
+  isPublic        Boolean    @default(false)
   title           String
   description     String
   category        String
   ownerIdentifier String
-  createdAt       DateTime       @default(now())
-  updatedAt       DateTime       @default(now())
+  createdAt       DateTime   @default(now())
+  updatedAt       DateTime   @default(now())
   userLists       UserList[]
   listItems       ListItem[]
-  UserListItem    UserListItem[]
 }
 model UserList {
   id             Int            @id @default(autoincrement())
@@ -45,10 +44,9 @@
   listItemId     Int
   userListId     Int
   status         ReadingStatus @default(NOT_STARTED)
   listItem       ListItem      @relation(fields: [listItemId], references: [id])
-  userList       List          @relation(fields: [userListId], references: [id])
-  UserList       UserList      @relation(fields: [userListId], references: [id])
+  userList       UserList      @relation(fields: [userListId], references: [id])
 }
 enum ReadingStatus {
   NOT_STARTED
```
