# Migration `20210103194833-added-list-owner-table`

This migration has been generated by Greg Murray at 1/3/2021, 2:48:33 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "List" DROP COLUMN "ownerIdentifier"

CREATE TABLE "ListOwner" (
    "ownerIdentifier" TEXT NOT NULL,
    "listId" INTEGER NOT NULL,

    PRIMARY KEY ("ownerIdentifier","listId")
)

CREATE UNIQUE INDEX "ListOwner_listId_unique" ON "ListOwner"("listId")

ALTER TABLE "ListOwner" ADD FOREIGN KEY("listId")REFERENCES "List"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210103072154-unique-fields-on-list-items..20210103194833-added-list-owner-table
--- datamodel.dml
+++ datamodel.dml
@@ -1,25 +1,33 @@
 datasource DS {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
   binaryTargets = "native"
 }
 model List {
-  id              Int        @id @default(autoincrement())
-  isPublic        Boolean    @default(false)
-  title           String
-  description     String
-  category        String
+  id          Int        @id @default(autoincrement())
+  isPublic    Boolean    @default(false)
+  title       String
+  description String
+  category    String
+  createdAt   DateTime   @default(now())
+  updatedAt   DateTime   @default(now())
+  userLists   UserList[]
+  listItems   ListItem[]
+  listOwner   ListOwner?
+}
+
+model ListOwner {
   ownerIdentifier String
-  createdAt       DateTime   @default(now())
-  updatedAt       DateTime   @default(now())
-  userLists       UserList[]
-  listItems       ListItem[]
+  listId          Int
+  list            List   @relation(fields: [listId], references: [id])
+
+  @@id([ownerIdentifier, listId])
 }
 model UserList {
   id             Int            @id @default(autoincrement())
```


